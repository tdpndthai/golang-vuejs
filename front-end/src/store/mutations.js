export const SetCustomer = (state, cus) => {
  state.customer = cus;
};

export const EditCustomer = (state, cus) => {
  state.customer = cus;
};

export const SetToken = (state, token) => {
  state.token = token;
};

export const SetLang = (state, lang) => {
  state.lang = lang;
};

export const SetPhoto = (state, photo) => {
  state.photo = photo;
};
